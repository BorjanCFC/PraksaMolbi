<header class="header">
  <div class="header-container">
    <h1 class="logo">🎓 Студентска Служба</h1>
    <nav>
      <% if (typeof student !== 'undefined' && student) { %>
        <span class="nav-user">👤 <%= student.ime %> <%= student.prezime %></span>
        <a href="/dashboard" class="nav-link">Dashboard</a>
        <a href="/logout" class="nav-link btn-logout">Одјава</a>
      <% } %>
    </nav>
  </div>
</header>
